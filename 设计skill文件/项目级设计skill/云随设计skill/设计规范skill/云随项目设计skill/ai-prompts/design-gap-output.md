# 设计缺口输出模板

当需求缺少核心样板、业务规则、权限规则、风险机制或关键流程时，停止自行设计复杂页面，并按以下格式输出。

```markdown
## 设计缺口

- 已能复用的部分：
- 缺失的核心页面或状态：
- 需要设计师确认的业务规则：
- 建议补充的 Figma 样板：
- 可先生成的非核心部分：
- 暂停生成的原因：
```

注意：

- 不把 AI 猜测当作已覆盖规则。
- 不把云随内部补全策略的临时补全写成正式云随规则。
- 不要求产品经理或研发补做 L0～L3 判断。
- 可先生成成熟部分时，必须明确哪些部分不是正式方案。
